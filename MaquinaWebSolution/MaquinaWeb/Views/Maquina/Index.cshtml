@model MaquinaWeb.Models.Troco
@{
    ViewBag.Title = "Gerador de Troco";

}

@using (Html.BeginForm("CalcularTroco", "Maquina", FormMethod.Post, new { id = "myForm" }))
{
    @Html.AntiForgeryToken()
    <div class="col-md-8">
        <h2>Geração de Troco na Máquina</h2>
    </div>
    <div class="col-md-4 tela">
        <div class="col-md-10 col-md-offset-1">
            @Html.LabelFor(troco => troco.Total, "Digite o troco desejado", new { @class = "labelTroco"})
            @Html.EditorFor(troco => troco.Total,"Digite o valor do Troco" ,new { htmlAttributes = new {@class = "inputTroco" } })
            @*<input type="number" step=0.01 class="inputTroco" placeholder="Digite" name="Total" />*@
        </div>
    </div>

    <div class="col-md-8 col-md-offset-2 btnCalcular">
        <button class="btn btn-primary" type="submit">Calcular Quantidade de Moedas</button>
    </div>

    <div class="col-md-10 col-md-offset-1 divMoedas">
        @foreach (var moeda in Model.NovasMoedas)
        {
            <div class="col-md-2 moedaEQuant" id="@Url.Content(moeda.Valor.ToString())">
                <img src="@Url.Content(moeda.srcImg)" class="moeda" />
            </div>
        }
    </div>
}

@*<div id="myModal1" class="modal hide" tabindex="-1" role="dialog">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h3>Modal</h3>

    </div>
    <div class="modal-body">Olá Mundo!!</div>
    <div class="modal-footer">
        <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
        <button class="btn btn-primary">Save changes</button>
    </div>
</div>*@

<div class="modal fade" id="retornoModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modalBody">
                
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">OK</button>
            </div>
        </div>
    </div>
</div>


    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script type="text/javascript">
        if ($('#myForm').length) {
            console.log("Cheguei1");
            $('#myForm').on('submit', function (e) {
                e.preventDefault();
                $.ajax({
                    url: $(this).attr("action"),
                    data: $(this).serialize(),
                    dataType: "json",
                    method: $(this).attr("method"),
                    cache: false,
                    success: function (data) {
                        console.log(" Aqui = " + data.Sucesso);
                        //$('#retornoModal').modal('show');
                        if (data.Sucesso == "S") {
                            var quantidades = "";
                            $('#1').append("<span class='quantMoeda'>"+data.Valores[0]+"</span>");
                            $('#5').append("<span class='quantMoeda'>" + data.Valores[1] + "</span>");
                            $('#10').append("<span class='quantMoeda'>" + data.Valores[2] + "</span>");
                            $('#25').append("<span class='quantMoeda'>" + data.Valores[3] + "</span>");
                            $('#50').append("<span class='quantMoeda'>" + data.Valores[4] + "</span>");
                            $('#100').append("<span class='quantMoeda'>"+data.Valores[5]+"</span>");
                            $('.moedaEQuant').css("display","block");

                            $('.modalBody').html("<span>" + data.Mensagem + "</span>");
                            console.log("Val = ", data.Valores);
                        }
                        else $('.modalBody').html("<span>"+data.Mensagem+"</span>");
                        $('#retornoModal').modal('show');
                    },
                    error: function (jqXHR, textStatus, errorThrown) {
                        @*Msg.Erro("@(Orion.net.Resources.Shared.Mensagens.Msg_err_Generico)");*@
                        console.log(jqXHR);
                        console.log(errorThrown);
                        Loader.hide();
                    },
                    progress: function (e) {
                        if (e.lengthComputable) {
                            $('.modalBody').html("<span>Processando</span>");
                            $('#retornoModal').modal('show');
                            //var pct = (e.loaded / e.total) * 100;
                            //Loader.percentual(pct.toPrecision(3));
                        }
                    }
                });
            });
        }




    </script>




















